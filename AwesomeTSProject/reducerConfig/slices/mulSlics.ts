import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    value: 0
}


const mulSlice = createSlice({
    name: 'add',
    initialState,
    reducers: {}
})


export default mulSlice.reducer